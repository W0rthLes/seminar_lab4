version: '3'
services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "8080:8080"
    volumes:
      - ./jenkins_data:/var/jenkins_home
  ntp:
    image: cturra/ntp
    restart: always
    privileged: true
    environment:
      - NTP_SERVERS=pool.ntp.org
volumes:
  jenkins_home:
